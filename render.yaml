services:
  - type: web
    name: playwright-scraper
    env: node
    plan: free
    buildCommand: ""                 # default “npm install” is fine
    startCommand: npm start          # runs node scraper.js
